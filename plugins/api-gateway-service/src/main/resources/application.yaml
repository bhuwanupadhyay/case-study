spring:
  application:
    name: ApiGatewayService
  profiles:
    active: dev
  zipkin:
    sender:
      type: web
management:
  endpoints:
    web:
      exposure:
        include: prometheus,health,info

logstash:
  host: localhost
  port: 5044