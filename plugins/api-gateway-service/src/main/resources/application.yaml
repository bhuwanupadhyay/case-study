spring:
  application:
    name: ApiGatewayService
  profiles:
    active: dev
  zipkin:
    sender:
      type: web
management:
  endpoints:
    web:
      exposure:
        include: prometheus,health,info
boot:
  logstash:
    enabled: true
    url: localhost:5044
